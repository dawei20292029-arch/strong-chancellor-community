# 测试结果

版本：1.0.0  
验证日期：2026-09-06  
状态：文件结构与安装实测通过；模型行为未测试

## 自动检查

| 检查 | 结果 |
|---|---|
| 官方 Skill 结构校验 | PASS |
| `agents/openai.yaml` 解析与字段约束 | PASS |
| `test-prompts.json` 语法与 5 条用例 | PASS |
| 私人词汇、本机绝对路径和疑似密钥扫描 | PASS，0 命中 |
| Markdown 相对链接检查 | PASS，0 失效 |
| 必需文件与占位符检查 | PASS |
| Windows PowerShell 5.1 在全新目录实际安装 | PASS，14 个文件与源文件哈希相同 |
| Git Bash 在全新目录实际安装 | PASS，14 个文件与源文件哈希相同 |
| 重复安装边界测试 | PASS，拒绝覆盖，文件哈希不变，无嵌套目录 |

旧版说明中的 `Copy-Item -Recurse -Force` 在目标目录已存在时会产生第二层同名文件夹，现改为安装前检查目标并拒绝重复安装。测试目录未使用真实用户的 Skills 目录。

这里的安装实测指文件部署，不代表已经在全新账号下启动 Codex 并验证模型响应。原生 macOS / Linux 尚未运行；Git Bash 测试不能替代这两类环境测试。

## 行为验收

`test-prompts.json` 定义五类核心场景。由于模型版本和运行环境会影响表述，行为测试按期望语义验收，不要求逐字匹配。

本次没有调用模型执行这五条提示词，不能把“用例可解析”解释为“5/5 行为通过”。跨模型语义回归、真实业务效果和新用户安装成功率均待验证。安装者可使用同一组提示词复测其实际环境。
